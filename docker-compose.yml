services:
  forexbot:
    build: .
    image: forextradebot:latest
    env_file:
      - .env
    restart: unless-stopped
    # args: [cycles, interval_seconds] — defaults: ∞ cycles, 300s (5 min) interval
    # command: ["100", "60"]   # 100 cycles every 60 seconds
